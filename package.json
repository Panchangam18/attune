{
  "name": "attune",
  "version": "0.1.5",
  "description": "Agent skill and live CSS runtime for Chromium desktop apps",
  "type": "module",
  "files": [
    "dist",
    "README.md",
    "SKILL.md",
    "agents",
    "examples",
    "themes",
    "scripts"
  ],
  "keywords": [
    "agent-skill",
    "electron",
    "css",
    "desktop-automation",
    "ui-customization"
  ],
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "attune": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "build:themes": "node scripts/build-themes.mjs",
    "build:arrakis": "node scripts/build-themes.mjs arrakis",
    "dev": "tsx src/cli.ts",
    "prepack": "npm run build && npm run build:themes",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
