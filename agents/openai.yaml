interface:
  display_name: "Attune"
  short_description: "Style desktop apps through semantic UI roles"
  default_prompt: "Use $attune to restyle an open Chromium desktop app through its semantic element map."
